# cf_ai_agentguard
The project: A chat app where you type a task for an AI agent, it breaks the task into steps, and each step gets checked against security rules you define. Steps that violate a rule get blocked. You see allow/block results in real time with an audit log.
